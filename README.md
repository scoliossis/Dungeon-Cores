dungeon room cores for scalehack, currently the core scanning has small issues, eg: old trap has 2 different entries with only 1 hash different, spawn has 3 entries and so does blood

dungeon routes aswell, why not store them on the same repo
